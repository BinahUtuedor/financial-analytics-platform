version: 2

models:
  - name: stg_stock_prices
    columns:
      - name: symbol
        tests:
          - not_null

      - name: trading_date
        tests:
          - not_null

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - symbol
              - trading_date